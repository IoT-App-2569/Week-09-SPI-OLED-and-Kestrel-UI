   # 9.1 สถาปัตยกรรมบัส SPI และโครงสร้างภายในคอนโทรลเลอร์ SSD1306

---
## 9.1.1 วิวัฒนาการและการเปรียบเทียบโพรโทคอลสื่อสาร (I2C vs SPI  Bus)

การแสดงผลสำหรับคอมพิวเตอร์ตั้งแต่อดีตเป็นต้นมา มีการพัฒนาการที่หลากหลาย เช่นใช้สัญญาณโทรทัศน์ สัญญาณ VGA จนกระทั่งมาถึงการใช้สัญญาณ HDMI ซึ่งแต่ละแบบก็มีข้อดีข้อเสียแตกต่างกันไป 

ในขณะเดียวกัน การแสดงผลสำหรับไมโครคอนโทรลเลอร์ในงาน IoT ก็มีการพัฒนาการที่หลากหลายเช่นกัน แต่โดยทั่วไปจะมีอินเทอร์เฟซให้ใช้ 2 แบบ คือ I2C และ SPI

### การอินเทอร์เฟซแบบ I2(squrze)C (Inter-Integrated Circuit) 
การอินเตอร์เฟสแบบ I2C ใช้สายสัญญาณ 2 เส้น คือ SDA และ SCL โดย SDA ใช้สำหรับส่งข้อมูล และ SCL ใช้สำหรับส่งสัญญาณนาฬิกา มีข้อดีคือใช้สายสัญญาณน้อยแต่ข้อเสียคือความเร็วในการส่งข้อมูลช้ากว่า SPI

<p align = "center">
<img src="Images/SSD1306_I2C_Interface.svg">
</p>


### การอินเทอร์เฟสแบบ 4-Wire SPI (Serial Peripheral Interface)
 
การอินเตอร์เฟสแบบ SPI ใช้สายสัญญาณ 4 เส้น คือ MOSI, SCK, CS, DC และ RES โดย MOSI ใช้สำหรับส่งข้อมูล และ SCK ใช้สำหรับส่งสัญญาณนาฬิกา มีข้อดีคือความเร็วในการส่งข้อมูลเร็วกว่า I2C ทำให้มีอัตราการรีเฟรชหน้าจอ (Frame Rate) ที่สูงกว่า 

<p align = "center">
<img src="Images/SSD1306_SPI_Interface.svg">
</p>




### ตารางเปรียบเทียบคุณสมบัติ

| คุณลักษณะ                      | I2C (SSD1306 4-pin)             | 4-Wire SPI (SSD1306 7-pin)                      |
| :----------------------------- | :------------------------------ | :---------------------------------------------- |
| **จำนวนสายสัญญาณ**             | 2 สาย (SDA, SCL)                | 4-5 สาย (MOSI, SCK, CS, DC, RES)                |
| **ความเร็วในการส่งข้อมูล**     | ช้า-ปานกลาง (100 kHz - 400 kHz) | **เร็วมาก (10 MHz - 20 MHz+)**                  |
| **อัตราการรีเฟรชหน้าจอ (FPS)** | 10 - 15 FPS (ค่อนข้างกระตุก)    | **50 - 60+ FPS (ลื่นไหล เหมาะกับ Realtime UI)** |

ในการทดลองที่ผ่านมา เรามีการสุ่มสัญญาณจาก Potentiometer และแสดงผลผ่าน kestrel ที่อัตรา 10 - 20 ครั้งต่อวินาที ซึ่งจะกระตุกถ้าต่อกับหน้าจอ OLED แบบ I2C ทั่วไป 

ดังนั้นในการทดลองนี้เราจะใช้ OLED ที่มีอินเตอร์เฟสแบบ 4-Wire SPI เพื่อให้ได้อัตราการรีเฟรชหน้าจอที่สูงพอที่จะแสดงผลกราฟิกแบบ Realtime UI สำหรับอัตราการสุ่มนั้นได้ โดยไม่ต้องเขียนโปรแกรมเพิ่มสำหรับการปรับความสัมพันธ์ของเฟรมเรตและอัตราการสุ่มข้อมูลให้แสดงผลแบบต่อเนื่อง

---

## 9.1.2 โครงสร้างฮาร์ดแวร์ 4-Wire SPI บนโมดูล OLED SSD1306

โมดูล OLED 0.96 นิ้วที่มีแถบขาเชื่อมต่อ 7 ขา ใช้มาตรฐานการเชื่อมต่อแบบ **4-Wire Serial Peripheral Interface (SPI)** โดยขาแต่ละขามีบทบาทเฉพาะเจาะจง ดังนี้

<p align = "center">
<img src="Images/SSD1306_OLED_Controller_SPI.svg">
</p>

### หน้าที่ของขา DC (Data / Command Select)
หัวใจสำคัญที่สุดที่ทำให้นักศึกษาเข้าใจสถาปัตยกรรมของคอนโทรลเลอร์หน้าจอ คือขา **DC**
- **เมื่อ DC = 0 (LOW)** 
  ชิปจะตีความไบต์ที่ส่งเข้าไปว่าเป็น **"Command Register"** เช่น การตั้งค่าคอนทราสต์, การเปิด Charge Pump, หรือการกำหนดตำแหน่งเคอร์เซอร์
- **เมื่อ DC = 1 (HIGH)** 
  ชิปจะตีความไบต์ที่ส่งเข้าไปว่าเป็น **"Graphic Data (พิกเซล)"** และจะนำข้อมูลนั้นเขียนลงในหน่วยความจำ GDDRAM ทันที

---

## 9.1.3 สถาปัตยกรรมหน่วยความจำกราฟิก 1 KByte (GDDRAM Architecture)

หน้าจอขนาด 0.96 นิ้ว มีความละเอียดทางกายภาพ $128 \times 64$ พิกเซล รวมทั้งสิ้น $8,192$ จุด  
เนื่องจากเป็นจอแบบขาว-ดำ (Monochrome) แต่ละจุดพิกเซลจึงต้องการข้อมูลเพียง **1 บิต** (0 = ดับ, 1 = สว่าง)

$$\text{ขนาดหน่วยความจำทั้งหมด} = \frac{128 \times 64 \text{ bits}}{8 \text{ bits/byte}} = 1,024 \text{ Bytes} = 1\text{ KByte}$$

### โครงสร้างแบ่งเป็น 8 Pages (Page 0 ถึง Page 7)
ชิป SSD1306 ไม่ได้จัดเก็บข้อมูลแบบแนวนอนเรียงกัน 128 บิต แต่จัดเก็บในลักษณะ **8 Pages ในแนวตั้ง** ดังนี้

```
           Column 0   Column 1   Column 2   ...   Column 127
          ┌──────────┬──────────┬──────────┬─────┬──────────┐
Page 0    │ Byte [0] │ Byte [1] │ Byte [2] │ ... │Byte[127] │ (Row 0 - 7)
          ├──────────┼──────────┼──────────┼─────┼──────────┤
Page 1    │Byte [128]│Byte [129]│   ...    │     │Byte[255] │ (Row 8 - 15)
          ├──────────┼──────────┼──────────┼─────┼──────────┤
Page 2    │   ...    │          │          │     │          │ (Row 16 - 23)
Page 3    │   ...    │          │          │     │          │ (Row 24 - 31)
Page 4    │   ...    │          │          │     │          │ (Row 32 - 39)
Page 5    │   ...    │          │          │     │          │ (Row 40 - 47)
Page 6    │   ...    │          │          │     │          │ (Row 48 - 55)
          ├──────────┼──────────┼──────────┼─────┼──────────┤
Page 7    │Byte [896]│   ...    │          │     │Byte[1023]│ (Row 56 - 63)
          └──────────┴──────────┴──────────┴─────┴──────────┘
```

### การกระจายบิตใน 1 ไบต์ (1 Vertical Byte)

ในแต่ละ Page ทุกๆ 1 ไบต์จะคุมพิกเซลในแนวตั้ง 8 จุดเสมอ
- บิตที่ 0 (LSB - บิตล่างสุด): ควบคุมพิกเซลด้านบนสุดของ Page
- บิตที่ 7 (MSB - บิตบนสุด): ควบคุมพิกเซลด้านล่างสุดของ Page

```
      LSB  Bit 0 ───► [Pixel Y = 0]
           Bit 1 ───► [Pixel Y = 1]
           Bit 2 ───► [Pixel Y = 2]
           Bit 3 ───► [Pixel Y = 3]
           Bit 4 ───► [Pixel Y = 4]
           Bit 5 ───► [Pixel Y = 5]
           Bit 6 ───► [Pixel Y = 6]
      MSB  Bit 7 ───► [Pixel Y = 7]
```

---

## 9.1.4 คณิตศาสตร์ Bitwise Manipulation บน Framebuffer ในแรมของ ESP32

ในการวาดภาพลงบนจอ เราจะไม่ส่งข้อมูลพิกเซลทีละจุดออกไปทางสาย SPI ให้เปลืองเวลา Overhead แต่เราจะสร้าง **Framebuffer ขนาด 1,024 ไบต์ขึ้นมาในแรมของ ESP32 ก่อน**

```c
uint8_t oled_buffer[1024]; // หน่วยความจำจำลองขนาด 1KB บนแรมของ ESP32
```

เมื่อเราต้องการจุดไฟพิกเซลที่พิกัด $(x, y)$ ใด ๆ
1. คำนวณหาว่าพิกัดนั้นตกอยู่ใน Page ใด: $\text{Page Index} = \lfloor y / 8 \rfloor$
2. คำนวณหาตำแหน่ง Offset ไบต์ในอาร์เรย์: $\text{Index} = x + (\text{Page Index} \times 128)$
3. คำนวณหาตำแหน่งบิตภายในไบต์นั้น: $\text{Bit Position} = y \pmod 8$
4. ใช้ตัวดำเนินการระดับบิต **Bitwise OR (`|=`)** เพื่อเปิดไฟ หรือ **Bitwise AND-NOT (`&= ~`)** เพื่อดับไฟ

```c
// ตัวอย่างฟังก์ชันเปิดไฟบนพิกเซล
void oled_draw_pixel(int x, int y, bool color) {
    if (x < 0 || x >= 128 || y < 0 || y >= 64) return; // ป้องกันเขียนเกินขอบเขตแรม

    int byte_index = x + (y / 8) * 128;
    int bit_offset = y % 8;

    if (color) {
        oled_buffer[byte_index] |= (1 << bit_offset);  // เปิดไฟ
    } else {
        oled_buffer[byte_index] &= ~(1 << bit_offset); // ดับไฟ
    }
}
```

หลังจากที่เราวาดเส้น วาดกล่อง หรือเขียนตัวอักษรลงใน `oled_buffer` จนเสร็จสิ้น เราจะเรียกคำสั่งส่งผ่านบัส SPI เพียงครั้งเดียว (**Block Transfer 1,024 Bytes**) เพื่ออัปเดตหน้าจอทั้งหมดภายในเสี้ยววินาที  ซึ่งข้อมูล 1 กิโลไบต์ใช้เวลาสื่อสารบนพอร์ต SPI ที่สั้นมาก ๆ
## 9.1.5 การวาดภาพบนจอโดยใช้หลัก Double Buffering

ในการสร้างกราฟิกสำหรับระบบสมองกลฝังตัวและงานแสดงผลทั่วไป ปัญหาคลาสสิกที่มักพบเมื่อมีการอัปเดตหน้าจออย่างรวดเร็ว (เช่น มาตรวัดเข็ม, แอนิเมชันแถบพลังงาน) คือ **อาการภาพกระพริบ (Screen Flickering)** หรือ **ภาพฉีกขาด (Screen Tearing)**

### ปัญหาของการวาดลงจอโดยตรง (Single Buffer Direct Draw)
หากโปรแกรมสั่งล้างหน้าจอแล้วค่อย ๆ ส่งคำสั่งวาดเส้น วาดกล่อง และเขียนตัวอักษรลงไปยังตัวควบคุมหน้าจอทีละส่วน สายตามนุษย์จะสังเกตเห็นจังหวะที่หน้าจอกำลัง "ถูกลบจนมืด" สลับกับจังหวะที่ "ค่อย ๆ วาดวัตถุใหม่ขึ้นมา" ทำให้เกิดการกระพริบที่ทำให้ผู้ใช้งานรู้สึกไม่สบายตา และทำให้ประสิทธิภาพการสื่อสารต่ำมากเพราะต้องส่งคำสั่งย่อยจำนวนมากผ่านบัสสื่อสาร

<p align = "center">
<img  src="Images/Single_Buffering_Problem.svg">
</p>

<p align = "center">
<img  src="Images/Single_Buffering_problem_in_OLED.svg">
</p>

---

### หลักการทำงานของ Double Buffering บน ESP32 และ SSD1306

แนวคิด **Double Buffering** แก้ปัญหานี้ด้วยการแบ่งหน่วยความจำภาพออกเป็น 2 ชั้น:

1. **Back Buffer (Off-Screen Canvas ในแรมของ ESP32)**
   - เป็นอาร์เรย์ `uint8_t oled_buffer[1024]` ที่จองไว้ในหน่วยความจำ RAM ของ ESP32
   - ซีพียูสามารถสั่งลบหน้าจอ วาดเส้นตาราง เคลื่อนย้ายตำแหน่งเข็มมาตรวัด หรือพิมพ์ตัวอักษรลงในบัฟเฟอร์นี้ได้อย่างอิสระ โดยที่การเปลี่ยนแปลงทั้งหมดยังไม่ปรากฏให้ผู้ใช้เห็น
2.  **Front Buffer (On-Screen Display ในชิป SSD1306)**
   - คือหน่วยความจำ **1024 Bytes GDDRAM** ที่ฝังอยู่ภายในตัวชิป SSD1306
   - วงจรสแกนหน้าจอ (Display Engine) จะดึงข้อมูลจากส่วนนี้ไปจุดเม็ดพิกเซลจริงบนแผงหน้าจอ OLED ตลอดเวลา
3. **การส่งถ่ายข้อมูลในจังหวะเดียว (Buffer Flush / Swap)**
   - เมื่อซีพียูวาดองค์ประกอบทั้งหมดลงใน Back Buffer เสร็จสิ้น 1 เฟรม จะสั่งฟังก์ชัน `oled_flush()` ส่งข้อมูล 1,024 ไบต์รวดเดียวผ่านบัส SPI ความเร็วสูง 10 MHz (ใช้เวลาเพียงประมาณ $0.8\text{ ms}$) เข้าไปเขียนทับ Front Buffer ทั้งหมดในพริบตาเดียว
   - ผลลัพธ์: ภาพบนหน้าจอจะเปลี่ยนฉากใหม่ทันทีอย่างราบเรียบ นิ่งสนิท และไร้การกระพริบ (**Flicker-Free Realtime UI**)

---

### แผนภาพแสดงการทำงานของ Double Buffering

<p align = "center">
<img src="Images/Double_Buffering_Concept.svg">
</p>

>[!info] การส่งข้อมูลผ่าน SPI บัสซึ่งทำงานที่ความเร็ว 10 MHz ใช้เวลาเพียงประมาณ $0.8 \text{ ms}$ เพื่อส่งข้อมูล 1,024 ไบต์ไปยัง Front Buffer ซึ่งทำให้ภาพบนหน้าจอเปลี่ยนฉากใหม่ทันทีอย่างราบเรียบ  ไร้การกระพริบ (**Flicker-Free Realtime UI**)

---

### วงจรชีวิตของ 1 เฟรมการแสดงผล (Frame Lifecycle)

ในลูปการทำงานจริง โปรแกรมจะปฏิบัติตามลำดับ 4 ขั้นตอนนี้ซ้ำ ๆ:

```c
while (1) {
    // 1. ล้าง Back Buffer ในแรมของ ESP32 (ยังไม่กระทบภาพบนจอจริง)
    oled_clear();

    // 2. วาดวัตถุและข้อมูลเซนเซอร์ชุดใหม่ลงบน Back Buffer
    oled_draw_rect(0, 0, 128, 64, true);
    oled_draw_string(10, 15, "ADC: 2048", true);
    oled_fill_rect(10, 30, bar_width, 10, true);

    // 3. ยิงข้อมูล 1KB ขึ้น Front Buffer ผ่านบัส SPI ในคำสั่งเดียว (หน้าจออัปเดตทันที)
    oled_flush();

    // 4. หน่วงเวลาตามอัตราเฟรมที่ต้องการ (เช่น 50ms = 20 เฟรมต่อวินาที)
    vTaskDelay(pdMS_TO_TICKS(50));
}
```