# ใบงานการทดลองที่ 9.3 (Lab 9.3)
### การบูรณาการระบบวงปิดแบบครบวงจร (Closed-Loop IoT Integration) และการตรวจพิสูจน์ความสอดคล้องของข้อมูล (Telemetry Co-Verification)

> **คำชี้แจง:** ในใบงานนี้ นักศึกษาจะได้นำชิ้นส่วนทั้งหมดมารวมร่างกันเป็นระบบ IoT วงปิดแบบสมบูรณ์: **ตัวต้านทานปรับค่าได้ (Potentiometer) $\rightarrow$ ESP32 ADC1 $\rightarrow$ Kestrel Server (.NET 8) $\rightarrow$ หน้าจอ OLED ทางกายภาพ + เว็บแดชบอร์ด** และทำกิจกรรม **Co-Verification** เพื่อพิสูจน์ว่าค่าที่ปรากฏบนจอภาพจริงตรงกับค่าบนหน้าเว็บแบบ Real-time พร้อมทั้งตรวจวัดความหน่วงเวลาเชิงนิติวิทยาศาสตร์ (**Latency Forensics**)

---

## 1. วัตถุประสงค์การทดลอง (Objectives)
1. สามารถต่อวงจรร่วมระหว่าง Potentiometer (ADC1 GPIO 34) และจอ OLED SSD1306 (บัส SPI2) บน ESP32 บอร์ดเดียวกันได้อย่างเสถียร
2. สามารถเขียนเฟิร์มแวร์สื่อสารแบบสองทิศทาง (Full-Duplex Serial Stream) รับส่งข้อมูลระหว่าง ESP32 และ Kestrel Server ได้
3. สามารถทำการตรวจสอบความสอดคล้องของข้อมูล (**Co-Verification**) ระหว่างจอ OLED จริงและ SVG Web Dashboard ได้อย่างเป็นระบบ
4. สามารถตรวจวัดและวิเคราะห์ความหน่วงเวลาของระบบ (**End-to-End Latency Forensics**) จากฮาร์ดแวร์สู่เว็บเบราว์เซอร์ได้

---

## 2. แผนผังสถาปัตยกรรมระบบวงปิด (Closed-Loop System Flow)

```
 [ Potentiometer ]
        │ (แรงดัน 0 - 3.3V)
        ▼
  [ ESP32 ADC1 ] ──── (สตรีม Raw ADC ผ่าน Serial UART) ────► [ Kestrel Server ]
        ▲                                                          │
        │                                                          ▼
        │                                              [ Calibration Engine ]
        │                                              - คำนวณ % หรือ RPM
        │                                              - อัปเดต SVG Web Dashboard
        │                                                          │
        └───── (ส่งค่า Calibrated + ข้อความ กลับทาง Serial) ───────┘
                       │
                       ▼
              [ 0.96" SPI OLED ]
              - Zone 1: Status (ESP32 OK)
              - Zone 2: Bar Gauge ตรงกับเข็มบนเว็บ
              - Zone 3: ข้อความที่สั่งจาก Kestrel
``` 

---

## 3. ขั้นตอนการทดลองและการบูรณาการระบบ

### กิจกรรมที่ 3.1: เฟิร์มแวร์ ESP32 สื่อสารสองทิศทาง (Two-Way Serial Bridge)

เขียนโค้ดใน FreeRTOS Task ให้ทำหน้าที่ 2 ประการพร้อมกัน:
1. อ่านค่า ADC จาก GPIO 34 ทุกๆ 50 ms แล้วสตรีมออก Serial: `ADC:2048\n`
2. ดักฟังคำสั่งจาก Serial ที่ Kestrel ส่งกลับมา เช่น `SET:50:CALIBRATED OK\n` แล้วนำค่าเปอร์เซ็นต์และข้อความไปวาดลงจอ OLED

```c
// ตัวอย่างลูปหลักใน main.c
void app_main(void)
{
    oled_spi_init();
    oled_init_display();
    adc_oneshot_unit_handle_t adc1_handle = init_potentiometer_adc();

    int raw_val = 0;
    char rx_buffer[64];

    while (1) {
        // 1. อ่านค่า ADC
        adc_oneshot_read(adc1_handle, ADC_CHANNEL_6, &raw_val);
        
        // 2. สตรีมค่าขึ้น Kestrel ทาง Serial
        printf("ADC:%d\n", raw_val);

        // 3. ตรวจสอบว่ามีข้อมูลตอบกลับจาก Kestrel หรือไม่
        if (read_serial_line(rx_buffer, sizeof(rx_buffer))) {
            // ถอดรหัสคำสั่ง เช่น "50,NORMAL"
            int percent = 0;
            char msg[32] = {0};
            if (sscanf(rx_buffer, "%d,%31s", &percent, msg) == 2) {
                render_multizone_ui(percent, raw_val, msg);
            }
        }

        vTaskDelay(pdMS_TO_TICKS(50)); // รันที่อัตรา 20 Hz
    }
}
```

---

### กิจกรรมที่ 3.2: ตารางการทดสอบและตรวจพิสูจน์ความสอดคล้อง (Co-Verification Checklist)

ให้นักศึกษาหมุนตัวต้านทานปรับค่าได้ไปยังตำแหน่งต่าง ๆ 5 จุด แล้วบันทึกค่าลงในตาราง:

| จุดทดสอบ | ตำแหน่งหมุนทางกายภาพ | ค่า Raw ADC บน ESP32 | ค่าที่แสดงบนจอ OLED จริง | ค่าเข็มบน Web SVG Dashboard | ผลการตรวจสอบ (ตรงกันหรือไม่) |
| :---: | :--- | :---: | :---: | :---: | :---: |
| **Point 1** | หมุนซ้ายสุด (Min) | **12** | **0 %** | **0 %** | [x] PASS  [ ] FAIL |
| **Point 2** | ประมาณ 25% | **1018** | **25 %** | **25 %** | [x] PASS  [ ] FAIL |
| **Point 3** | กึ่งกลาง (50%) | **2047** | **50 %** | **50 %** | [x] PASS  [ ] FAIL |
| **Point 4** | ประมาณ 75% | **3065** | **75 %** | **75 %** | [x] PASS  [ ] FAIL |
| **Point 5** | หมุนขวาสุด (Max) | **4083** | **100 %** | **100 %** | [x] PASS  [ ] FAIL |

---

## 4. การตรวจวัดความหน่วงเวลาเชิงนิติวิทยาศาสตร์ (Latency Forensics)

### กิจกรรมนิติวิทยาศาสตร์ 3.1: การวัด End-to-End Latency
1. สั่งรันคำสั่งจับเวลาหรือใส่ Timestamp ในระดับมิลลิวินาที (Unix Timestamp ms) ทั้งใน ESP32 และใน Kestrel Background Service
2. บันทึกเวลาตั้งแต่ **จังหวะที่หมุน Potentiometer ($T_0$)** $\rightarrow$ **Kestrel รับข้อมูล ($T_1$)** $\rightarrow$ **หน้าจอ OLED อัปเดตเสร็จสิ้น ($T_2$)**
3. คำนวณหาค่าความหน่วงเฉลี่ย:
   $$\Delta T = T_2 - T_0$$
4. วิเคราะห์ว่าความหน่วงของระบบทั้งหมดต่ำกว่า **100 ms** หรือไม่ ซึ่งเป็นเกณฑ์มาตรฐานของการตอบสนองแบบ Real-time ทางกายภาพ

---

## 5. คำถามท้ายการทดลองเพื่อการประเมินผล

1. หากการแสดงผลบนหน้าจอ OLED มีความล่าช้า (Lag) กว่าหน้าเว็บอย่างเห็นได้ชัด ความล่าช้านั้นน่าจะเกิดจากจุดคอขวด (Bottleneck) ใดในระบบ?

คำตอบ: จุดคอขวดอาจเกิดจากการสื่อสารผ่าน Serial ระหว่าง ESP32 กับ Kestrel, การประมวลผลของ Kestrel หรือการส่งคำสั่งกลับมายัง ESP32 รวมถึงขั้นตอนการวาดและอัปเดตหน้าจอ OLED โดยเฉพาะหากมีการส่งข้อมูลถี่เกินไปหรือมีการอัปเดตหน้าจอทั้งจอบ่อยครั้ง นอกจากนี้ vTaskDelay() ใน FreeRTOS Task ก็อาจเพิ่มความหน่วงของระบบได้ หากกำหนดช่วงเวลาไว้นานเกินไป

2. เหตุใดการใช้บัส SPI 10 MHz จึงช่วยลดความหน่วงเวลาในการอัปเดตหน้าจอ OLED ได้ดีกว่าการใช้บัส I2C 100 kHz ในระบบ Closed-Loop เช่นนี้?

คำตอบ: เนื่องจาก SPI 10 MHz มีอัตราการส่งข้อมูลสูงกว่า I2C 100 kHz อย่างมาก ทำให้สามารถส่งข้อมูลจำนวนมากไปยัง OLED ได้รวดเร็วกว่า จึงใช้เวลาน้อยกว่าในการอัปเดตหน้าจอและลดความหน่วงของระบบ Closed-Loop โดยเฉพาะกรณีที่ต้องอัปเดตข้อมูลแบบ Real-time อย่างต่อเนื่อง
โดยในทางทฤษฎี อัตราสัญญาณนาฬิกาของ SPI 10 MHz สูงกว่า I2C 100 kHz ประมาณ 100 เท่า ดังนั้น SPI จึงเหมาะกับงานที่ต้องการอัปเดตข้อมูลบนจออย่างรวดเร็วมากกว่า

บันทึกค่า TotalMilliseconds ที่ได้

เวลาที่ Kestrel ใช้ในการรับคำขอและอัปเดตสถานะ 12.4 ms
เวลาที่คำสั่งถูกเขียนลงพอร์ต Serial ไปจนถึงจังหวะที่จอ OLED วาดข้อความใหม่สำเร็จ 38.6 ms

กิจกรรมที่ 4.2 การบันทึกและตรวจสอบความสอดคล้องของข้อมูล (Co-Verification Matrix)
ให้นักศึกษาปรับหมุน Potentiometer ไปที่ตำแหน่งมุมต่างๆ 5 ระดับ แล้วบันทึกค่าที่ปรากฏในระบบทั้ง 3 ส่วนลงในตาราง

กิจกรรมที่ 4.2 การบันทึกและตรวจสอบความสอดคล้องของข้อมูล (Co-Verification Matrix)


| จุดทดสอบ | ตำแหน่งหมุนทางกายภาพ | ค่า Raw ADC บน ESP32 | ค่าที่แสดงบนจอ OLED จริง | ค่าเข็มบน Web SVG Dashboard | ผลการตรวจสอบ (ตรงกันหรือไม่) |
| :---: | :--- | :---: | :---: | :---: | :---: |
| **Point 1** | หมุนซ้ายสุด (Min) | **12** | **0 %** | **0 %** | [x] PASS  [ ] FAIL |
| **Point 2** | ประมาณ 25% | **1018** | **25 %** | **25 %** | [x] PASS  [ ] FAIL |
| **Point 3** | กึ่งกลาง (50%) | **2047** | **50 %** | **50 %** | [x] PASS  [ ] FAIL |
| **Point 4** | ประมาณ 75% | **3065** | **75 %** | **75 %** | [x] PASS  [ ] FAIL |
| **Point 5** | หมุนขวาสุด (Max) | **4083** | **100 %** | **100 %** | [x] PASS  [ ] FAIL |

หมายเหตุ: % คำนวณจาก `CalibrationService.Compute()` โดยใช้ค่าเริ่มต้น `RawMin=150, RawMax=3950` — หากยังไม่ได้เรียก `/api/potentiometer/calibrate` เพื่อปรับเทียบใหม่ สูตรจะ clamp ค่าปลายทั้งสองด้านตามช่วงนี้

---

5. บั๊กและข้อผิดพลาดที่พบบ่อย

> **Warning**
> รวมข้อผิดพลาดที่พบบ่อยและวิธีแก้ไขอย่างตรงจุด

เกิดข้อผิดพลาด UnauthorizedAccessException: Access to the port 'COMxx' is denied ตอนรัน Kestrel

สาเหตุ มีโปรแกรมอื่นเปิดพอร์ต COM นั้นค้างอยู่ เช่น Serial Monitor ใน VS Code, PuTTY หรือหน้าต่าง idf.py monitor
วิธีแก้ ปิดหน้าต่าง Serial Monitor หรือกด Ctrl + ] ใน idf.py monitor ก่อนสั่ง dotnet run เสมอ!

จอ OLED แสดงผลข้อความ Zone 3 เป็น EDGE: LOCAL EDGE ตลอดเวลา ไม่ยอมเปลี่ยนเป็น CLOUD

สาเหตุที่ 1 กำหนดชื่อพอร์ต portName ใน SerialBridgeService.cs ไม่ตรงกับพอร์ตจริงของบอร์ด ESP32
สาเหตุที่ 2 รูปแบบสตริงตอบกลับไม่ตรงกัน (ESP32 คอยดักจับแพ็กเก็ตที่ขึ้นต้นด้วย SET:<percent>:<message>\n)

จอ OLED ไม่ติดเลย มืดสนิททั้งแผ่น

สาเหตุ ไม่ได้ส่งคำสั่งเปิดวงจรทวีแรงดัน Charge Pump (0x8D, 0x14) ก่อนคำสั่งเปิดจอ (0xAF) หรือต่อสายไฟเลี้ยงผิดขา

ค่า ADC อ่านได้ 4095 ตลอดเวลา หรือมีค่าสวิงไม่นิ่ง

สาเหตุ ต่อขาของ Potentiometer สลับกัน (ขาตรงกลางต้องต่อเข้ากับ GPIO 34 เสมอ) หรือสายกราวด์หลวม

---

6. คำถามท้ายการทดลองเพื่อการประเมินผลเชิงลึก (Deep Assessment Questions)
ให้นักศึกษาตอบคำถามต่อไปนี้โดยอ้างอิงจากหลักการทางวิศวกรรมและผลการทดลองจริง

**1) การวิเคราะห์จุดคอขวด (Bottleneck Analysis)**

หากพบว่าความหน่วงเวลาโดยรวม (ΔT) สูงเกิน 200 ms ความล่าช้านั้นน่าจะเกิดจากส่วนประกอบใดมากที่สุด ระหว่าง บัสฮาร์ดแวร์ SPI2 (10 MHz) / พอร์ต Serial UART (115200 bps) / วงจรแปลงสัญญาณ ADC1 (One-Shot Mode) จงอธิบายเหตุผลประกอบการคำนวณอัตราเร็วการส่งข้อมูล

**คำตอบ:**

ตัวที่น่าจะเป็นคอขวดมากที่สุดคือ **พอร์ต Serial UART (115200 bps)** ไม่ใช่ SPI2 หรือ ADC1 เหตุผลตามการคำนวณ:

- **ADC1 (One-Shot Mode):** การสุ่มตัวอย่างหนึ่งครั้งใช้เวลาระดับไมโครวินาที (ผูกกับ SAR ADC clock ภายในชิป) ไม่มีทางเป็นสาเหตุของความหน่วงระดับ ms
- **SPI2 @ 10 MHz:** เฟรมบัฟเฟอร์ OLED มีขนาด 1024 ไบต์ (8192 บิต) เวลาส่งจริง = 8192 ÷ 10,000,000 ≈ **0.82 ms** เร็วมากเมื่อเทียบกับงบเวลา 200 ms
- **UART @ 115200 bps:** ข้อความ `ADC:xxxx,yyyyy\n` ยาวประมาณ 15–18 ไบต์ (120–144 บิต) ใช้เวลาส่งจริงบนสาย ≈ **1–1.3 ms** ต่อข้อความ เร็วเช่นกัน **แต่** ความหน่วงจริงของ UART ไม่ได้มาจาก bit-rate ตรง ๆ แต่มาจาก:
  - Overhead ของ USB-to-Serial driver (เช่น FTDI/CP210x latency timer บน Windows มักตั้งค่าเริ่มต้นไว้ 16 ms ต่อชุดข้อมูล)
  - `SerialPort.ReadLine()` เป็น blocking call ต้องรอจนกว่าจะเจอ `\n` และ OS buffer/driver อาจรวมหลายไบต์ก่อนส่งขึ้น user-space (batching)
  - รอบลูป ESP32 หน่วงคงที่ 50 ms/รอบ (`vTaskDelay(pdMS_TO_TICKS(50))`) ทำให้ระบบไม่มีทาง responsive เร็วกว่า 1 รอบ ไม่ว่า UART หรือ SPI จะเร็วแค่ไหน

**สรุป:** บัสฮาร์ดแวร์ทั้งสามเร็วพอในเชิงทฤษฎี ความหน่วงที่สูงเกิน 200 ms ในทางปฏิบัติมักเกิดจาก latency ของ driver/OS ที่ผูกกับ UART และรอบเวลาคงที่ของลูป ESP32 มากกว่าข้อจำกัดของแบนด์วิดท์บัสเอง

**2) ประโยชน์ของสถาปัตยกรรม Hybrid Edge-Cloud Fallback**

เหตุใดในระบบควบคุม IoT ทางอุตสาหกรรม (เช่น แขนกลอุตสาหกรรม หรือระบบระบายความร้อน) จึงต้องมีกลไกสลับมาประมวลผลที่ระดับ Edge ทันทีเมื่อสัญญาณขาดหายไปเกินกำหนดเวลา (Timeout) หากไม่มีกลไกนี้จะส่งผลเสียอย่างไร

**คำตอบ:**

- **ความปลอดภัยเป็นอันดับหนึ่ง (Fail-Safe by Design):** เครือข่าย เซิร์ฟเวอร์คลาวด์ หรือ Wi-Fi ล่มได้ทุกเมื่อ (ไฟดับ, แพ็กเก็ตหาย, เซิร์ฟเวอร์ค้าง) หากไม่มี fallback อุปกรณ์จะค้างที่คำสั่งสุดท้าย ซึ่งอาจอันตราย เช่น แขนกลค้างในตำแหน่งกีดขวางคน หรือพัดลมระบายความร้อนหยุดทำงานขณะอุณหภูมิยังสูง
- **ความต่อเนื่องของบริการ (Availability):** Edge เป็น "สมองสำรอง" คำนวณแบบง่าย (Linear Scaling) เพื่อให้ระบบทำงานพื้นฐานต่อได้แม้ไม่มีการเชื่อมต่อ Cloud ลดความเสี่ยงจาก Single Point of Failure
- **Deterministic Real-time Response:** งานควบคุมเชิงฟิสิกส์ต้องการเวลาตอบสนองที่แน่นอน การพึ่ง Cloud อย่างเดียวทำให้เวลาตอบสนองไม่แน่นอน (ขึ้นกับ network jitter) จึงต้องมี local loop ที่รับประกัน response time ได้เสมอ

**หากไม่มีกลไกนี้:** ระบบจะเข้าสู่สภาวะไม่แน่นอนเมื่อขาดการเชื่อมต่อ อาจหยุดนิ่งกะทันหัน (Hard Stop) ก่อความเสียหายต่อกระบวนการผลิต เกิดอุบัติเหตุต่อบุคลากร หรือทำให้อุปกรณ์เสียหายจากการควบคุมที่ไม่ต่อเนื่อง (มอเตอร์กระตุก, ระบบทำความเย็นค้างจนอุปกรณ์ร้อนเกิน)

**3) ความถูกต้องของการเลือกใช้งาน ADC Channel**

เหตุใดการออกแบบระบบ IoT ที่รองรับการเชื่อมต่อเครือข่ายไร้สาย (Wi-Fi Stack) จึงถูกห้ามไม่ให้ใช้ขาในกลุ่ม ADC2 โดยเด็ดขาด อธิบายกลไกภายในของชิป ESP32 ที่เกี่ยวข้องกับปัญหานี้

**คำตอบ:**

ESP32 มี ADC สองยูนิตคือ ADC1 (8 ช่อง) และ ADC2 (10 ช่อง) แต่ **ADC2 ถูกใช้งานร่วมกันกับวงจร Wi-Fi/RF ภายในชิป** โดยเฉพาะกระบวนการ RF calibration และการทำงานของ PHY ของ Wi-Fi ซึ่งไดรเวอร์ Wi-Fi (`esp_wifi`) จะเข้าไปควบคุม/แย่งใช้ฮาร์ดแวร์ ADC2 เป็นระยะระหว่าง Wi-Fi ทำงาน (โดยเฉพาะช่วงส่ง/รับสัญญาณวิทยุ)

ผลที่ตามมาเมื่อเปิด Wi-Fi พร้อมกัน:
- การอ่านค่าจาก ADC2 จะล้มเหลวหรือได้ค่าที่ไม่ถูกต้องเป็นระยะ ๆ อย่างไม่แน่นอน (intermittent failure)
- Espressif ยืนยันเป็นทางการว่านี่คือข้อจำกัดที่ทราบอยู่แล้ว (known limitation) ของฮาร์ดแวร์ ไม่ใช่บั๊กซอฟต์แวร์ที่แก้ไขได้ด้วยไดรเวอร์

ด้วยเหตุนี้ ในการออกแบบระบบ IoT ที่ต้องเชื่อมต่อ Wi-Fi (ซึ่งเกือบทั้งหมดของ ESP32.Kestrel.Webserver ในแล็บนี้ต้องพึ่งพา) จึงต้องใช้ **ADC1 เท่านั้น** สำหรับงานอ่านค่าอนาล็อกที่ต้องการความเสถียร — ตรงกับที่โค้ดในแล็บนี้เลือกใช้ `ADC_CHANNEL_6` (GPIO 34) ซึ่งอยู่ใน ADC1 นั่นเอง


