# สรุปคำตอบและรายงานผล Lab 9

วิดีโอประกอบการทดลอง: [YouTube Shorts](https://youtube.com/shorts/iWld47gdw7g?si=BQ55lLhdN2OlQr_B)

## Lab 9.1: SPI OLED

1. **ทำไมตัว `H` ใช้ 5 ไบต์ และแต่ละไบต์ควบคุมอะไร?**  
   เพราะฟอนต์กว้าง 5 คอลัมน์ จึงใช้ 5 ไบต์ แต่ละไบต์ควบคุมพิกเซลแนวตั้ง 8 จุดของ 1 คอลัมน์
2. **หากสลับสาย D0 และ D1 จะเกิดอะไรขึ้น?**  
   D0 คือ clock และ D1 คือ data เมื่อสลับกัน SPI สื่อสารผิด จอจะแสดงผลไม่ได้
3. **ทำไมแก้ `s_oled_buffer` แล้วภาพยังไม่เปลี่ยนทันที?**  
   เพราะแก้เฉพาะข้อมูลใน RAM ต้องเรียก `oled_flush()` เพื่อส่งข้อมูลไป OLED

## Lab 9.2: Kestrel API

1. **ทำไมควรคำนวณสเกลที่ Kestrel Server?**  
   ปรับค่าได้ง่ายจากเว็บ ลดงานบน ESP32 และใช้ค่าปรับเทียบร่วมกันได้
2. **หากไม่ตรวจ `RawMax <= RawMin` จะเกิดอะไรขึ้น?**  
   อาจเกิด `DivideByZeroException` หรือคำนวณค่าผิด ทำให้ API ตอบ error ได้
3. **ทำไม POST ที่ไม่มี `Content-Type: application/json` จึงได้ 415?**  
   Server ไม่รู้ว่า body เป็น JSON จึงไม่ยอมแปลงข้อมูล และตอบ `415 Unsupported Media Type`

## Lab 9.3: Closed-Loop IoT

### ตารางการทดสอบและตรวจพิสูจน์ความสอดคล้อง

| จุดทดสอบ | ตำแหน่งหมุนทางกายภาพ | Raw ADC | OLED | Web SVG | ผล |
| :---: | :--- | :---: | :---: | :---: | :---: |
| Point 1 | หมุนซ้ายสุด (Min) | 0 | 0 % | 0 % | [X] PASS |
| Point 2 | ประมาณ 25% | 1115 | 25 % | 25 % | [X] PASS |
| Point 3 | กึ่งกลาง (50%) | 2050 | 50 % | 50 % | [X] PASS |
| Point 4 | ประมาณ 75% | 3000 | 75 % | 75 % | [X] PASS |
| Point 5 | หมุนขวาสุด (Max) | 4095 | 100 % | 100 % | [X] PASS |

1. **หาก OLED หน่วงกว่าหน้าเว็บ จุดคอขวดน่าจะอยู่ที่ใด?**  
   อาจอยู่ที่ Serial, การวาด OLED, การส่งข้อมูลผ่านเว็บ หรือ loop ที่หน่วงนานเกินไป
2. **ทำไม SPI 10 MHz หน่วงน้อยกว่า I2C 100 kHz?**  
   SPI ส่งข้อมูลเร็วกว่า จึงอัปเดต framebuffer 1 KB ได้เร็วและหน่วงน้อยกว่า

## Lab 9.4: Troubleshooting

### แบบฟอร์มรายงานผลการชันสูตรทางวิศวกรรม

```text
================================================================================
                    ENGINEERING FORENSIC INCIDENT REPORT
================================================================================
1. รหัสนักศึกษา / ผู้รายงาน: 67030142
2. กรณีศึกษาข้อผิดพลาด: [X] Case 1   [ ] Case 2   [ ] Case 3   [ ] Case 4
3. อาการที่ตรวจพบ: ESP32 ทำงาน แต่ OLED มืดสนิทและไม่แสดงพิกเซล
4. สมมติฐาน: ขา RES ค้าง LOW หรือ Charge Pump ยังไม่ถูกเปิด
5. หลักฐาน:
   - โค้ดที่ผิด: oled_send_cmd(0x8D); oled_send_cmd(0x10);
   - Opcode 0x10 ปิด Charge Pump จึงไม่มีแรงดันสำหรับ OLED
6. การแก้ไขและผล:
   เปลี่ยน 0x10 เป็น 0x14 และส่ง 0xAF เพื่อเปิดจอ
   หลัง flash ใหม่ OLED แสดงผลได้ตามปกติ
================================================================================
```

1. **หากสาย SCK (GPIO 18) หลุด จะเกิดอะไรขึ้น?**  
   จอจะไม่อัปเดตหรือมืด เพราะไม่มี clock ให้ SSD1306 รับคำสั่งและข้อมูล SPI
2. **ทำไม Hex Dump น่าเชื่อถือกว่าดูจอด้วยตา?**  
   Hex Dump แสดงค่าจริงใน framebuffer จึงตรวจบิตและตำแหน่งหน่วยความจำได้ตรงกว่า
