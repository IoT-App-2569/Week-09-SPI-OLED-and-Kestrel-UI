# คำตอบคำถามท้ายการทดลอง Lab 9

## Lab 9.1: SPI OLED

1. ตัว `H` กว้าง 5 คอลัมน์ จึงใช้ 5 ไบต์ แต่ละไบต์ควบคุมพิกเซลแนวตั้ง 8 จุดของ 1 คอลัมน์
2. D0 คือ clock และ D1 คือ data; ถ้าสลับกัน SPI สื่อสารผิด จอจะแสดงผลไม่ได้
3. การวาดเปลี่ยนเฉพาะข้อมูลใน RAM; ต้องเรียก `oled_flush()` เพื่อส่งข้อมูลไปยัง OLED

## Lab 9.2: Kestrel API

1. Kestrel ปรับค่าได้ง่าย เปลี่ยนการตั้งค่าจากเว็บได้ และลดงานคำนวณบน ESP32
2. จะเกิด `DivideByZeroException` หรือคำนวณค่าผิด ทำให้ API ตอบ error หรือหยุดทำงานได้
3. Server ไม่รู้ว่า body เป็น JSON จึงไม่ยอมแปลงข้อมูล และตอบ `415 Unsupported Media Type`

## Lab 9.3: Closed-Loop IoT

1. คอขวดอาจอยู่ที่ Serial, การวาด OLED, การส่งข้อมูลผ่านเว็บ หรือ loop ที่หน่วงนานเกินไป
2. SPI 10 MHz ส่งข้อมูลได้เร็วกว่า I2C 100 kHz มาก จึงอัปเดต framebuffer 1 KB ได้เร็วและหน่วงน้อยกว่า

## Lab 9.4: Troubleshooting

1. จอจะไม่อัปเดตหรือมืด เพราะไม่มีสัญญาณ clock ให้ SSD1306 รับคำสั่งและข้อมูลผ่าน SPI
2. Hex Dump แสดงค่าจริงใน framebuffer จึงตรวจบิตและตำแหน่งหน่วยความจำได้ตรงกว่าการดูภาพบนจอ
