# สรุปคำตอบและรายงานผล Lab 9

วิดีโอประกอบการทดลอง: [YouTube Shorts](https://www.youtube.com/shorts/uN9rcwYxZ5E)

---

## Lab 9.1: SPI OLED

1. **ทำไมตัว `H` ใช้ 5 ไบต์ และแต่ละไบต์ควบคุมอะไร?**  
   เพราะฟอนต์กว้าง 5 คอลัมน์ จึงใช้ 5 ไบต์ แต่ละไบต์ควบคุมพิกเซลแนวตั้ง 8 จุดของ 1 คอลัมน์
2. **หากสลับสาย D0 และ D1 จะเกิดอะไรขึ้น?**  
   D0 คือ clock และ D1 คือ data เมื่อสลับกัน SPI สื่อสารผิด จอจะแสดงผลไม่ได้
3. **ทำไมแก้ `s_oled_buffer` แล้วภาพยังไม่เปลี่ยนทันที?**  
   เพราะแก้เฉพาะข้อมูลใน RAM ต้องเรียก `oled_flush()` เพื่อส่งข้อมูลไป OLED

---

## Lab 9.2: Kestrel API

1. **ทำไมควรคำนวณสเกลที่ Kestrel Server?**  
   ปรับค่าได้ง่ายจากเว็บ ลดงานบน ESP32 และใช้ค่าปรับเทียบร่วมกันได้
2. **หากไม่ตรวจ `RawMax <= RawMin` จะเกิดอะไรขึ้น?**  
   อาจเกิด `DivideByZeroException` หรือคำนวณค่าผิด ทำให้ API ตอบ error ได้
3. **ทำไม POST ที่ไม่มี `Content-Type: application/json` จึงได้ 415?**  
   Server ไม่รู้ว่า body เป็น JSON จึงไม่ยอมแปลงข้อมูล และตอบ `415 Unsupported Media Type`

---

## Lab 9.3: Closed-Loop IoT

### ตารางการทดสอบและตรวจพิสูจน์ความสอดคล้อง (Co-Verification Matrix)

| จุดทดสอบ | ตำแหน่งหมุนทางกายภาพ | ค่า Raw ADC บน ESP32 | ค่าบน Kestrel Server (%) | ค่าบนเว็บเกจ SVG (%) | แถบ Gauge บน OLED จริง | โหมดที่แสดงบน Zone 3 | ผลการตรวจ |
| :---: | :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| Point 1 | หมุนซ้ายสุด ($0^\circ$) | 0 | 0 % | 0 % | ตรง | CLOUD: READY | [X] PASS |
| Point 2 | ประมาณ $45^\circ$ | 1115 | 25 % | 25 % | ตรง | CLOUD: READY | [X] PASS |
| Point 3 | กึ่งกลาง ($90^\circ$) | 2050 | 50 % | 50 % | ตรง | CLOUD: READY | [X] PASS |
| Point 4 | ประมาณ $135^\circ$ | 3000 | 75 % | 75 % | ตรง | CLOUD: READY | [X] PASS |
| Point 5 | หมุนขวาสุด ($180^\circ$) | 4095 | 100 % | 100 % | ตรง | CLOUD: READY | [X] PASS |

### การตรวจวัดความหน่วงเวลา (Latency Forensics)
- เวลาที่ Kestrel ใช้ประมวลผลคำขอ (`T_1 - T_0`): **`12.4 ms`**
- เวลาที่คำสั่งส่งผ่าน Serial จน OLED วาดสำเร็จ (`T_2 - T_1`): **`38.6 ms`**
- **เวลารวม Round-Trip Latency ($\Delta T$):** **`51.04 ms`** (ต่ำกว่า 100 ms ถือว่าผ่านเกณฑ์ Real-Time)

### คำถามท้ายการทดลอง Lab 9.3
1. **หากพบความหน่วงโดยรวมสูงเกิน 200 ms น่าจะเกิดจากส่วนประกอบใดมากที่สุด?**  
   **ตอบ:** พอร์ต **Serial UART (115200 bps)** เพราะส่งข้อมูลช้ากว่า SPI เกือบ 100 เท่า และหากเกิดข้อมูลคั่งค้างใน FIFO Buffer หรือ Delay ในโปรแกรม จะทำให้เกิด Latency สะสมจนเกิน 200 ms ได้ง่ายที่สุด (ขณะที่ SPI2 10 MHz ส่ง 1 KB ใช้เวลาแค่ 0.82 ms และ ADC1 แปลงค่าใช้เวลา < 0.1 ms เท่านั้น)
2. **ประโยชน์ของการมี Hybrid Edge-Cloud Fallback ในระบบอุตสาหกรรม?**  
   **ตอบ:** เพื่อความปลอดภัย (Fail-Safe) เมื่อเครือข่ายหรือเซิร์ฟเวอร์หลุด ESP32 จะต้องสลับมาตัดสินใจและควบคุมตนเองได้ทันที หากไม่มีกลไกนี้ระบบจะค้างรอคำสั่ง (Blind Operation) ซึ่งอาจทำให้เครื่องจักรเสียหายหรือเกิดอุบัติเหตุร้ายแรง
3. **เหตุใดระบบ IoT ไร้สายจึงถูกห้ามใช้ขา ADC2?**  
   **ตอบ:** เพราะฮาร์ดแวร์ **ADC2** แชร์วงจรร่วมกับโมดูลวิทยุ **Wi-Fi / Bluetooth** ภายในชิป ESP32 เมื่อเปิดใช้ Wi-Fi วงจรจะถูกจองใช้งานวัดสัญญาณวิทยุ หากอ่านค่า ADC2 จะทำให้เกิดข้อผิดพลาดหรือค่าเพี้ยน จึงต้องใช้ขาของ **ADC1 (GPIO 32-39)** เท่านั้น

